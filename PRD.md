# Markdown笔记工具 PRD

## 一、产品概述
一款轻量化本地Markdown笔记工具，基于浏览器LocalStorage存储，无需后端、无需登录，主打编辑实时预览、代码语法高亮，满足日常学习、技术笔记、文案记录等轻量化场景，适配PC端与手机端使用。

## 二、核心功能
1. 三栏布局页面
整体采用固定三栏结构，左栏为笔记操作列表区、中栏为Markdown编辑区、右栏为实时预览区，编辑与预览实时联动展示。
2. 笔记创建与编辑
- 支持新建空白笔记，可自定义修改笔记标题、编辑Markdown正文内容
- 支持主流Markdown语法渲染，停止输入后自动保存，切换/关闭页面兜底保存，防止内容丢失
3. 笔记搜索与管理
- 笔记列表按更新时间倒序展示，高亮区分当前选中笔记
- 支持标题、正文模糊搜索，无匹配内容展示空状态提示
- 支持单条笔记删除，删除二次确认，数据删除后不可恢复
4. 代码块语法高亮
预览区支持JS、TS、HTML、CSS、Java、Python、SQL等主流开发语言代码高亮，默认深色主题，代码自动换行防溢出，编辑区保留原始文本格式。
5. 本地数据持久化
所有笔记数据存储在浏览器LocalStorage，刷新、重启浏览器数据不丢失，异常场景自动初始化空数据，保障功能正常使用。

## 三、设计与交互要求
- 界面简洁轻量化，无冗余弹窗、无多余功能干扰
- 实时预览低延迟，编辑响应流畅不卡顿
- 关键操作有状态反馈（删除确认、空状态提示）
- 兼容Chrome、Edge、Firefox主流最新浏览器，适配PC端与手机端

## 四、移动端适配
1. 单视图切换
屏幕宽度小于768px时，由PC三栏布局切换为单视图模式，列表、编辑、预览三视图全屏切换，默认进入列表页，点击笔记进入编辑页，顶部工具栏支持返回列表、编辑/预览切换。
2. 触屏交互适配
- 删除、更多按钮常显，不再依赖鼠标悬停
- 更多菜单子菜单（导出）改为点击展开，窄屏下以下拉方式展示
- 触控目标不小于38px，编辑区字号不低于16px，防止iOS聚焦时页面自动放大
3. 数据与限制
- 数据仍存储于浏览器LocalStorage，与PC端各自独立，无跨设备同步
- 导出文件在iOS Safari上表现为打开而非保存，属已知平台差异