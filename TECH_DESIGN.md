# Markdown笔记工具 技术设计文档

## 技术栈
采用前端纯静态技术方案，无后端服务、无数据库，轻量化部署、开箱即用。

## 项目结构
采用轻量化分层结构，逻辑清晰、低耦合，便于维护与迭代。

## 数据管理
### 3.1 核心数据结构
单条笔记数据结构，统一标准化存储：
```
{
  id: String,        // 唯一标识（时间戳+随机数）
  title: String,     // 笔记标题
  content: String,   // Markdown 原文内容
  createTime: String,// 创建时间
  updateTime: String // 最后更新时间
}
```

### 3.2 存储方案
- 存储介质：LocalStorage，永久本地存储，页面刷新、重启不丢失
- 存储格式：JSON 数组，统一存取、解析、序列化
- 数据隔离：单独key管理笔记数据，避免全局污染

### 3.3 核心数据操作逻辑
- 初始化：页面加载读取本地数据，无数据则初始化空数组
- 新增笔记：生成唯一ID，默认标题，新增后置顶存储
- 编辑更新：500ms防抖自动更新内容与更新时间，实时落盘
- 搜索过滤：前端内存过滤，匹配标题、正文关键词
- 删除笔记：删除对应数据项，重新写入本地存储，不可恢复
- 异常容错：本地数据格式错误时，自动重置为空数据，避免页面报错
### 3.4 状态管理规则
全局统一管理笔记列表、当前选中笔记，所有组件共享状态，保证编辑、预览、列表数据一致性，避免数据不同步问题。

## 响应式与移动端适配
### 4.1 断点策略
- 屏幕宽度 max-width: 767px 时进入移动端单视图模式，PC三栏布局保持不变
- 触屏设备（无hover能力）通过 @media (hover: none) 单独适配，删除/更多按钮常显
### 4.2 视图切换机制
- body 上维护 m-view-list / m-view-editor / m-view-preview 三个状态类，由CSS控制各栏显隐
- JS 端通过 matchMedia 检测断点，监听 change 事件在切换视口时同步状态
- 移动端各视图占满 100dvh（回退100vh），适配移动浏览器地址栏伸缩
### 4.3 移动端交互改造
- 更多菜单子菜单由hover展开改为点击展开（more-menu-parent.open 控制），窄屏下子菜单改为静态下拉
- 兜底保存补充 pagehide 事件，弥补iOS上 beforeunload 不可靠的问题
- 编辑区字号16px，防止iOS聚焦时页面自动放大
- 新建笔记、点击列表项后自动进入编辑视图